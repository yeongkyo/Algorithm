#include <iostream>

using namespace std;

int main() {
    int A, B;

    // 세로 길이 A와 가로 길이 B를 입력받음
    cin >> A >> B;

    // 넓이(A * B)를 계산하여 출력
    cout << A * B << endl;

    return 0;
}